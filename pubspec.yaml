name: finances
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons:  

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/image/

flutter_icons:    
  ios: true
  android: true
  image_path_android: assets/image/android.png
  image_path_ios: assets/image/ios.png
  adaptive_icon_background: assets/image/background.png
  adaptive_icon_foreground: assets/image/foregroundn.png
  